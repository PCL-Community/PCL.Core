using PCL.Core.App;
using PCL.Core.App.Tasks;

namespace PCL.Core.Minecraft;

/*
public static class DlSourceHandler {
    public static bool DlSourceNetworkPreferMojang = false;
    
    public static bool DlSourcePreferMojang => Config.Tool.Download.FileSourceSolution == 2 
        ? true
        : Config.Tool.Download.FileSourceSolution == 1 && DlSourceNetworkPreferMojang;

    private static object LoadClientList() {
        TaskBase<object> task = new("加载下载源列表", (_, _) => {
            var clientList = new object();
            return clientList;
        });
        task.
    }
}
*/
